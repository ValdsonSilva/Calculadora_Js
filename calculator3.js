function displaynum(n1){
    //através da propriedade "form" podemos acessar qualquer elemento
    //filho da tag "form" através do nome desse elemento
    calculator.display.value = calculator.display.value + n1;
};